const LOCAL_STORAGE_PREFIX = 'IggyPope';

export const LOCAL_STORAGE_KEY = `${LOCAL_STORAGE_PREFIX}-search-term`;

const API_BASE_URL = 'https://swapi.dev/api';

export const API_PEOPLE_URL = `${API_BASE_URL}/people`;

export const ITEMS_PER_PAGE = 10;
